# Email Configuration Example
# Copy these values to your .env file if you want to use environment variables
# Otherwise, use configure-email.php script or database configuration

# Email Driver (smtp, sendmail, mailgun, etc)
MAIL_MAILER=smtp

# SMTP Host
# Gmail: smtp.gmail.com
# Outlook: smtp-mail.outlook.com
# Yahoo: smtp.mail.yahoo.com
MAIL_HOST=smtp.gmail.com

# SMTP Port
# TLS: 587
# SSL: 465
MAIL_PORT=587

# Email Username (your email address)
MAIL_USERNAME=your-email@gmail.com

# Email Password
# For Gmail: Use App Password (https://myaccount.google.com/apppasswords)
# For Yahoo: Use App Password
# For Outlook: Use regular password
MAIL_PASSWORD=your-app-password-here

# Encryption (tls or ssl)
MAIL_ENCRYPTION=tls

# From Address (sender email)
MAIL_FROM_ADDRESS=your-email@gmail.com

# From Name (sender name)
MAIL_FROM_NAME="${APP_NAME}"

# Queue Connection (for background email sending)
# Use 'database' for production, 'sync' for development
QUEUE_CONNECTION=database
